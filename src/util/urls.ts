export const apiBase = 'http://192.168.1.205:3010';

export const loginNeededUrl = `${apiBase}/login-needed`;
export const fetchAndSaveTokens = `${apiBase}/fetch-and-save-tokens`;

export const pauseUrl = `${apiBase}/pause`;
export const playUrl = `${apiBase}/play`;
export const playPlaylistUrl = `${apiBase}/play-playlist`;
export const prevUrl = `${apiBase}/prev`;
export const nextUrl = `${apiBase}/next`;
export const devicesUrl = `${apiBase}/devices`;
export const transferUrl = `${apiBase}/transfer`;
export const getPlaylistsUrl = `${apiBase}/playlists`;
export const setShuffleUrl = `${apiBase}/shuffle`;
